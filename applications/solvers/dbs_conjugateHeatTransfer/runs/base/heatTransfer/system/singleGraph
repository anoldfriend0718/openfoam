/*--------------------------------*- C++ -*----------------------------------*\
  =========                 |
  \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox
   \\    /   O peration     | Website:  https://openfoam.org
    \\  /    A nd           | Version:  7
     \\/     M anipulation  |
-------------------------------------------------------------------------------
Description
    Writes graph data for specified fields along a line, specified by start
    and end points.

\*---------------------------------------------------------------------------*/




type            sets;
libs            ("libsampling.so");

interpolationScheme cellPoint;

fields  (T);

setFormat   csv;


writeControl    writeTime;

sets
(
    obstacle1_1
    {
        type    lineUniform; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.040 0.0 0.0);
        end    (0.040 0.005 0.0);
        nPoints 51;
    }

    obstacle1_1_cell
    {
        type    lineCell; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.040 0.0 0.0);
        end    (0.040 0.0049 0.0);
    }

    obstacle1_1_cell_adj
    {
        type    lineCell; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.03995 0.0 0.0);
        end    (0.03995 0.0049 0.0);
    }

    
    obstacle1_2
    {
        type    lineUniform; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.040 0.005 0.0);
        end    (0.045 0.005 0.0);
        nPoints 51;
    }

    obstacle1_2_cell
    {
        type    lineCell; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.040 0.005 0.0);
        end    (0.045 0.005 0.0);
    }

    obstacle1_2_cell_adj
    {
        type    lineCell; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.040 0.00505 0.0);
        end    (0.045 0.00505 0.0);
    }

    obstacle1_3
    {
        type    lineUniform; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.045 0.005 0.0);
        end    (0.045 0.000 0.0);
        nPoints 51;
    }

    obstacle1_3_cell
    {
        type    lineCell; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.045 0.0049 0.0);
        end    (0.045 0 0.0);
    }

    obstacle1_3_cell_adj
    {
        type    lineCell; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.04505 0.0049 0.0);
        end    (0.04505 0 0.0);
    }


    obstacle2_1
    {
        type    lineUniform; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.050 0.0 0.0);
        end    (0.050 0.005 0.0);
        nPoints 51;
    }

    obstacle2_1_cell
    {
        type    lineCell; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.050 0.0 0.0);
        end    (0.050 0.0049 0.0);
    }

    obstacle2_1_cell_adj
    {
        type    lineCell; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.04995 0.0 0.0);
        end    (0.04995 0.0049 0.0);
    }


    obstacle2_2
    {
        type    lineUniform; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.050 0.005 0.0);
        end    (0.055 0.005 0.0);
        nPoints 51;
    }

    obstacle2_2_cell
    {
        type    lineCell; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.050 0.005 0.0);
        end    (0.055 0.005 0.0);
    }

    obstacle2_2_cell_adj
    {
        type    lineCell; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.050 0.00505 0.0);
        end    (0.055 0.00505 0.0);
    }

    obstacle2_3
    {
        type    lineUniform; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.055 0.005 0.0);
        end    (0.055 0.000 0.0);
        nPoints 51;
    }

    obstacle2_3_cell
    {
        type    lineCell; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.055 0.0049 0.0);
        end    (0.055 0 0.0);
    }

    obstacle2_3_cell_adj
    {
        type    lineCell; // lineCell, lineCellFace
        axis    distance;    // x, y, z, xyz
        start  (0.05505 0.0049 0.0);
        end    (0.05505 0 0.0);
    }
    
);

// ************************************************************************* //
